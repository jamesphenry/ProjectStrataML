# Configuration Templates

This directory contains declarative configuration templates for ProjectStrataML:

- `datasets/` - Dataset configuration templates
- `models/` - Model configuration templates  
- `training/` - Training run configuration templates
- `sweeps/` - Hyperparameter sweep configurations

See TFC-0002 for schema specifications.